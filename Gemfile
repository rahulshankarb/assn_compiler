source 'https://rubygems.org'

gem 'rails', '3.2.11'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'pg' # Postgres
gem 'sorcery' # authentication
gem 'simple_form' # forms made easy
gem "kaminari" #paginate
#gem 'faker' #generate fake things
gem 'annotate', ">=2.5.0.pre1" # annotate 
gem "cancan" # admin authentication
#gem 'aws-sdk'# attachments in aws s3
gem 'thin' #fix warnings msgs
gem 'quiet_assets' #fix assets msgs
gem 'paperclip'# attachments  control
gem 'colorize' #colors for debug!!
gem 'delayed_job_active_record' #backgrounds jobs
#gem 'wicked_pdf' # PDF generate
#gem 'wkhtmltopdf-binary' #PDF generate
gem 'private_pub' # auto reload
#gem 'humanizer' # nice captcha
#gem 'roadie' # send email with html
gem 'version_gemfile' #to add version numbers to your Gemfile gems.
gem 'debase'



#Styles
  gem 'jquery-ui-themes'
  gem 'jquery-ui-rails'
  gem 'twitter-bootstrap-rails', '2.1.0'



# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'
  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

group :development do
  gem 'better_errors'
  gem 'binding_of_caller'
  gem 'rspec-rails'
  gem 'factory_girl_rails'
end


group :test do
  gem 'rspec-rails'
  gem 'factory_girl_rails'
  gem 'faker'
  gem 'capybara'
  gem 'guard-rspec'
  gem 'launchy'
end